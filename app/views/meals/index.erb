<p>coucou</p>
<%= @search.latitude %>
<%= @search.longitude %>

<%= Cooker.near([@search.latitude, @search.longitude], @search.distance) %>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
