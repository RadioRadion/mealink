<%= @user.address %>
<%= @user.username %>

<%= cl_image_tag @user.photo.key, height: 200, width: 200, crop: :fill, class: 'rounder-circle' %>

<% if @user == current_user %>
  <%= link_to "edit", edit_user_path(current_user) %>
<% end %>
